# Supabase Configuration
# Copie este arquivo para .env.local e preencha com seus valores reais
# 
# Comando PowerShell: Copy-Item env.local.example .env.local
# Comando Linux/Mac: cp env.local.example .env.local

# Project URL (encontre no Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://qigbblypwkgflwnrrhzg.supabase.co

# Anon Key (encontre no Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_anon_key_aqui

# Service Role Key (encontre no Supabase Dashboard → Settings → API)
# ⚠️ IMPORTANTE: Esta chave tem acesso total ao banco (bypass RLS)
# NUNCA exponha esta chave publicamente ou no código frontend
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_aqui

# Biologix API Credentials (para Edge Function - opcional para migração)
BIOLOGIX_USERNAME=l|DEMO|47349438
BIOLOGIX_PASSWORD=sua_senha_aqui
BIOLOGIX_SOURCE=100
BIOLOGIX_PARTNER_ID=4798042LW

